# Kubernetes Secret storing MariaDB credentials
# Use Secrets for sensitive data — ConfigMaps are not suitable for passwords.
apiVersion: v1
kind: Secret
metadata:
  name: mariadb-secret
  namespace: nextcloud
type: Opaque
stringData:
  MARIADB_ROOT_PASSWORD: "raspi"
  MARIADB_USER: "nextcloud"
  MARIADB_PASSWORD: "raspi"
  MARIADB_DATABASE: "nextcloud"

#helm repo add smallstep https://smallstep.github.io/helm-charts/  < add smallstep repo 
#helm install step-ca smallstep/step-certificates  < install with helm
level=WARN msg="unable to find exact version; falling back to closest available version" chart=step-certificates requested="" selected=1.29.0
NAME: step-ca
LAST DEPLOYED: Thu Dec  4 10:46:53 2025
NAMESPACE: default
STATUS: deployed
REVISION: 1
DESCRIPTION: Install complete
NOTES:
Thanks for installing Step CA.

1. Get the PKI and Provisioner secrets running these commands:
   kubectl get -n default -o jsonpath='{.data.password}' secret/step-ca-step-certificates-ca-password | base64 --decode
   kubectl get -n default -o jsonpath='{.data.password}' secret/step-ca-step-certificates-provisioner-password | base64 --decode
2. Get the CA URL and the root certificate fingerprint running this command:
   kubectl -n default logs job.batch/step-ca

3. Delete the configuration job running this command:
   kubectl -n default delete job.batch/step-ca
